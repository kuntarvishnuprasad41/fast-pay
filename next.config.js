/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io', 'api.vishnuprasadkuntar.me', 'https://example.com', 'example.com']
  }
};

module.exports = nextConfig;
